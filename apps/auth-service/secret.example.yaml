apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: togglemaster
type: Opaque
stringData:
  MASTER_KEY: "REPLACE_ME"
  DATABASE_URL: "postgres://togglemaster:REPLACE_ME@RDS_ENDPOINT:5432/auth_db?sslmode=require"